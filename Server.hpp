# include "common.hpp"

typedef struct location {
    // 들어가야 하는 정보 정하기 
}   loction;

class Server 
{
    public :
        void run(void){
        }

    private :
        std::vector<location> locations;
        std::string m_host;
        std::string m_port;
        
}